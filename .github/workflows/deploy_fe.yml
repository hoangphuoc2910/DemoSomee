name: Deploy Frontend Only

on:
  # 1. Cho phép bấm nút chạy thủ công
  workflow_dispatch:

  # 2. Tự chạy khi push code
  push:
    branches:
      - main
      - master
    paths:
      - "Frontend/**" # Chỉ chạy khi sửa file trong Frontend

jobs:
  deploy-fe:
    runs-on: ubuntu-latest
    name: 🎉 Deploy HTML Frontend
    steps:
      - name: 🚚 Get latest code
        uses: actions/checkout@v4

      - name: 📂 Sync Frontend to InfinityFree
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: ${{ secrets.IF_HOST }}
          username: ${{ secrets.IF_USER }}
          password: ${{ secrets.IF_PASS }}

          # Lấy code từ thư mục Frontend
          local-dir: ./Frontend/

          # Đẩy vào htdocs (chung nhà với Backend)
          server-dir: htdocs/

          # Không xóa file của Backend
          dangerous-clean-slate: false
