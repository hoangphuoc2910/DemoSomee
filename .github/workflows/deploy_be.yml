name: Deploy Backend Only

on:
  # 1. Cho phép bấm nút chạy thủ công (Quan trọng để test)
  workflow_dispatch:

  # 2. Tự chạy khi push code
  push:
    branches:
      - main
      - master # Thêm dòng này đề phòng nhánh của bạn tên là master
    paths:
      - "Backend/**" # Chỉ chạy khi sửa file trong Backend

jobs:
  deploy-be:
    runs-on: ubuntu-latest
    name: 🚀 Deploy PHP Backend
    steps:
      - name: 🚚 Get latest code
        uses: actions/checkout@v4

      - name: 📂 Sync Backend to InfinityFree
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: ${{ secrets.IF_HOST }}
          username: ${{ secrets.IF_USER }}
          password: ${{ secrets.IF_PASS }}

          # Lấy code từ thư mục Backend
          local-dir: ./Backend/

          # Đẩy vào htdocs
          server-dir: htdocs/

          # Không xóa file của Frontend
          dangerous-clean-slate: false
