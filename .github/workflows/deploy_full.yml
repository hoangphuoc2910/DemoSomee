name: Deploy ALL to InfinityFree

on:
  push:
    branches:
      - main

jobs:
  web-deploy:
    name: 🚀 Deploy Website
    runs-on: ubuntu-latest
    steps:
      - name: 🚚 Get latest code
        uses: actions/checkout@v4

      - name: 📂 Sync files to htdocs
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: ${{ secrets.IF_HOST }}
          username: ${{ secrets.IF_USER }}
          password: ${{ secrets.IF_PASS }}

          # BẮT BUỘC: InfinityFree chỉ chạy web trong thư mục htdocs
          server-dir: htdocs/

          # Tùy chọn: Xóa file rác trên host nếu trên GitHub không có
          # Lưu ý: Nếu bạn có lưu ảnh upload trên host thì để false
          dangerous-clean-slate: false
