name: Deploy ALL to InfinityFree

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  web-deploy:
    name: 🚀 Deploy Website
    runs-on: ubuntu-latest
    steps:
      - name: 🚚 Get latest code
        uses: actions/checkout@v4

      - name: 📂 Sync Frontend to htdocs
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: ${{ secrets.IF_HOST }}
          username: ${{ secrets.IF_USER }}
          password: ${{ secrets.IF_PASS }}

          # Sửa thành viết thường để khớp với folder của bạn
          local-dir: ./frontend/
          server-dir: htdocs/
          dangerous-clean-slate: false

      - name: 📂 Sync Backend to htdocs
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: ${{ secrets.IF_HOST }}
          username: ${{ secrets.IF_USER }}
          password: ${{ secrets.IF_PASS }}

          # Sửa thành viết thường để khớp với folder của bạn
          local-dir: ./backend/
          server-dir: htdocs/
          dangerous-clean-slate: false
